<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labkids.portal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Lab Kids"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LabKids">

        <!-- Main TWA Launcher Activity -->
        <activity
            android:name="com.google.android.manifestapp.MainActivity"
            android:exported="true"
            android:label="Lab Kids"
            android:theme="@style/Theme.LabKids.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="portal-atividades.onrender.com"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Home Screen Widget Receiver (Estilo Duolingo) -->
        <receiver
            android:name=".LabKidsWidget"
            android:exported="true"
            android:label="Lab Kids Widget 🤖">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lab_kids_widget_info" />
        </receiver>

    </application>

</manifest>
